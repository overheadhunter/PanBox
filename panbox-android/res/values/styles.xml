<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="@android:style/Theme.Holo.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
         <item name="android:actionBarStyle">@style/customActionBar</item>
         <item name="android:textViewStyle">@style/customTextView</item>
         <item name="android:buttonStyle">@style/customButton</item>
    </style>
    
    <!-- Globally set the text size of the textView -->
    <style name="customTextView" parent="android:Widget.TextView">
        <item name="android:textSize">18sp</item>
    </style>
    
    <!-- Globally set the top and bottom padding of button -->
    <style name="customButton" parent="android:Widget.Button">
    	<item name="android:paddingTop">16dp</item>
        <item name="android:paddingBottom">20dp</item>
        <item name="android:textSize">15sp</item>
    </style>
    
     <!-- ActionBar theme. -->
    <style name="customActionBar" parent="@android:style/Theme.Holo.Light">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
         <item name="android:background">#D8D8D8</item>
         <item name="android:height">63dip</item>
    </style>
    
</resources>
